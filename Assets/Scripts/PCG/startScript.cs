using UnityEngine;
using System.Collections;

public class startScript : MonoBehaviour {


	//Script controls the placement of player and exit point, however this may require integration with the map/mesh gen scripts rather than inheritence

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
